import random

number = random.randint(1, 10)
choice = random.choice(["apple", "banana", "orange"])

import math

math.sqrt(9)
import datetime

today = datetime.date.today()
print(today)

# how this works it directly inteacts with the operating systems
import os

current_dir = os.getcwd()
print(current_dir)
